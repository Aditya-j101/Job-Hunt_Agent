# LLM + agent orchestration
# huggingface_hub>=0.23.0
groq>=0.9.0
langchain-anthropic>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0

# data validation / schemas
pydantic>=2.6.0

# resume / file parsing
pdfplumber>=0.11.0

# UI
streamlit>=1.38.0

# config / env vars
python-dotenv>=1.0.0

# testing
pytest>=8.0.0
pytest-mock>=3.14.0

fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9

#StaticFiles
aiofiles>=23.0.0

# optional: only needed if you build the v2 embedding-based matching stretch goal
# sentence-transformers>=3.0.0